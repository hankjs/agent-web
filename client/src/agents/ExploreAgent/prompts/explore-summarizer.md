压缩以下探索摘要，保留关键事实，删除冗余细节。目标：不超过 500 字。

当前摘要：
{{current_summary}}

新发现：
{{new_findings}}

输出格式（使用以下结构，纯文本，不要 JSON 包裹）：

## 已确认决策
- [用户确认的决策和偏好，提炼为结构化结论，如"存储形式：本地 Markdown"]

## 技术事实
- [关键架构决策、文件路径、函数名、类名等代码标识符（原样保留，不要缩写）]

## 已排除路径
- [尝试过但失败/被否决的方案，简述原因，如"方案A：用Redis缓存 → 用户否决，偏好本地方案"]

## 待确认
- [尚未解决的问题]

规则：
- 保留用户确认的决策和偏好
- 保留关键架构决策和技术事实
- 保留文件路径、函数名、类名等代码标识符（原样保留，不要缩写）
- 保留失败的尝试和错误信息——它们指示了已排除的方向，防止重复探索
- 删除重复信息
- 删除过程性描述（"我读了 X 文件"）
- 删除已被后续发现覆盖的旧信息
- 如果某个分类下没有内容，省略该分类标题
