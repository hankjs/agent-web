压缩以下探索摘要，保留关键事实，删除冗余细节。目标：不超过 500 字。

当前摘要：
{{current_summary}}

新发现：
{{new_findings}}

输出压缩后的摘要（纯文本，不要 JSON 包裹）。保留：
- 用户确认的决策和偏好（将问答对提炼为结构化结论，如"存储形式：本地 Markdown"，不保留原始问答原文）
- 关键架构决策和技术事实
- 尚未解决的问题（标记为"待确认"）
- 文件路径、函数名、类名等代码标识符（原样保留，不要缩写）

删除：
- 重复信息
- 过程性描述（"我读了 X 文件"）
- 已被后续发现覆盖的旧信息
